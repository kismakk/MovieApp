import React from 'react'
import { StyledButton } from './StyledButton'

const UserButton = () => {
  return (
    <>
      <button style={StyledButton}>User</button>
    </>
  )
}

export default UserButton