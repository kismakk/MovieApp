import React from 'react'
import { StyledButton } from './StyledButton'

const GroupButton = () => {
  return (
    <>
      <button style={StyledButton}>Groups</button>
    </>
  )
}

export default GroupButton