import React from 'react'
import { StyledButton } from './StyledButton'

const LogoutButton = () => {
  return (
    <>
      <button style={StyledButton}>Logout</button>
    </>
  )
}

export default LogoutButton